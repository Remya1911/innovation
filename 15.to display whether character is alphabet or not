#include <stdio.h>

int main(void) {
char ch;
scanf("%c",&ch);
if(((ch>='a')&&(ch<='z'))||((ch>='A')&&(ch=<'Z')))
		printf("input %c is alphabet",ch);
		else
		printf("input not alphbet");
	// your code goes here
	
	return 0;
}
