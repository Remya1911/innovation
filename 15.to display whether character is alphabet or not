#include <stdio.h>

int main(void) {
char ch;
scanf("%c",&ch);
if(((ch>=65)&&(ch<='90'))||((ch>=97)&&(ch<=122)))
		printf("input %c is alphabet",ch);
		else
		printf("input not alphbet");
	// your code goes here
	
	return 0;
}
