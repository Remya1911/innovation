#include <stdio.h>

int main(void) {
int num;
scanf("%d",&num);
if(num>0)
	printf("number %d is postiver",num);
	else if(num<0)
		printf("number %d is negative",num);
		else
		printf("zero");
	// your code goes here
	
	return 0;
}
